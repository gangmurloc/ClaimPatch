Task: verbalize a set of facts as one natural answer paragraph without exposing
their dependency structure.

Write a concise answer to the question using every supplied fact exactly once
in meaning. Distribute related facts naturally across the paragraph. Do not
explain that one fact follows from another. Do not mention claim IDs, nodes,
graphs, dependencies, labels, slots, metadata, or this instruction. Do not add
facts or calculations that are not already present. Do not present a bullet
list. Vary the sentence organization according to the style hint.

Before returning, silently verify the paragraph against the complete supplied
fact list. When supplied, each of the following must be stated explicitly
rather than left to inference from another fact: each individual metric, the
numeric gap (including a zero gap), the qualitative comparison, the evaluation
split, and the source/version.

Hard surface constraint: the paragraph must not contain any of these
case-insensitive phrases:
"therefore", "thus", "because", "which means", "based on", "derived from",
"computed from", "resulting in", "indicating that", "consequently".

Keep all facts present without explaining their dependency relationships. The
checks and constraints must not be described in the output.

Return JSON only:
{{
  "prose": "one natural paragraph"
}}

Question:
{question}

Facts in shuffled order:
{claims}

Style hint:
{style_hint}

