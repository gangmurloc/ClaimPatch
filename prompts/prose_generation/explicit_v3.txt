Task: verbalize a set of facts as one natural answer paragraph.

Write a concise answer to the question using every supplied fact exactly once
in meaning. You may use natural connective phrases when they improve fluency,
but do not mention claim IDs, nodes, graphs, dependencies, labels, slots,
metadata, or this instruction. Do not add facts or calculations that are not
already present. Do not present a bullet list. Vary the sentence organization
according to the style hint.

Before returning, silently verify the paragraph against the complete supplied
fact list. When supplied, each of the following must be stated explicitly
rather than left to inference from another fact: each individual metric, the
numeric gap (including a zero gap), the qualitative comparison, the evaluation
split, and the source/version. This check must not be described in the output.

Return JSON only:
{{
  "prose": "one natural paragraph"
}}

Question:
{question}

Facts in shuffled order:
{claims}

Style hint:
{style_hint}

