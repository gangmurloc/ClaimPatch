seed: 13
run_id: p1_prompted_mock_100
stage_title: DECAP P1 Prompted Structured Mock Experiment
output_dir: outputs/p1_prompted
dataset:
  size: 100
  domain: benchmark_results
  sequential_steps: 3
systems:
  - p1_prompted_mock
  - unstructured_selective_edit
  - attribute_no_graph
  - descendant_all
  - full_regeneration
bootstrap:
  samples: 1000
bootstrap_metrics:
  - dependency_complete_success
  - collateral_edit_rate
  - residual_stale_rate
  - patch_precision
  - patch_recall
primary_system: p1_prompted_mock
bootstrap_against:
  - unstructured_selective_edit
  - attribute_no_graph
  - descendant_all
interpretation: "P1 prompted-mock exercises prompt loading, structured JSON parsing, Pydantic validation, and SemanticPatch generation through the same executor/evaluator as P0. Because the backend is deterministic mock, this is a plumbing/reproducibility test, not evidence of real LLM performance."
